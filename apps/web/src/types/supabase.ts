/**
 * Supabase generated types.
 *
 * Regenerate after schema changes:
 *   pnpm db:types
 * (requires the Supabase CLI + a linked project, or a local `supabase start`).
 *
 * This is a placeholder so the app type-checks and runs BEFORE a Supabase
 * project is connected. Once generated, real table/view/enum types replace the
 * empty records below and all Supabase clients become strictly typed.
 */
export type Database = {
  public: {
    Tables: Record<string, never>;
    Views: Record<string, never>;
    Functions: Record<string, never>;
    Enums: Record<string, never>;
    CompositeTypes: Record<string, never>;
  };
};

export type DatabasePublic = Database['public'];
